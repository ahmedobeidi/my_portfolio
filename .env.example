# Base environment config

APP_ENV=dev
APP_SECRET=change_this_to_a_real_secret
APP_DEBUG=1

# Database connection example
DATABASE_URL="mysql://db_user:db_password@127.0.0.1:3306/db_name"

# Mailer config (example)
MAILER_DSN=smtp://localhost